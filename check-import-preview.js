const fs = require('node:fs');
const assert = require('node:assert/strict');
const html = fs.readFileSync('./index.html', 'utf8');
const source = fs.readFileSync('./api/workbook.js', 'utf8');
assert.match(html, /id="importPreviewDialog"/);
assert.match(html, /id="confirmImportPreview"/);
assert.match(html, /pendingImportPayload,confirm:true/);
assert.match(html, /Semua baris Excel asli/);
assert.match(source, /saveSourceRows/);
assert.match(source, /source_sheet_rows/);
assert.match(source, /payload\.confirm !== true/);
assert.match(source, /conflicts/);
assert.doesNotMatch(html, /duplikat dilewati/);
console.log('import preview, archive, and conflict checks: OK');
